# 评测报告（Evaluation Report）

> ⚠️ **口径提示**：本报告的评测集已于 2026-09-12 重写（改为本作内科场景，memory 3 条 + compression 3 条）。
> 下表数字属于**重写前的旧场景集**，反映的是运行时能力（记忆保持 / 压缩率），**不适用于当前场景集**。
> 要对当前场景集重新取值，跑 `node scripts/eval/runner.ts`。

- 模式：live（模型 `deepseek-v4-flash-0731`，OpenAI 兼容网关）
- 时间：2026-08-16（两批运行合并）
- 评测集：12 个 memory 场景（多轮剧情 × 4 轮 + 4 事实探询）+ 8 个 compression 场景（长剧情压缩）
- 指标口径：
  - 记忆保持率 = 探询回复中命中事实 keyword 的比例（关键词级判定，未计同义改写，**保守口径**）；
  - 压缩 token 节省 = 1 − 压缩后 token / 原文 token（3 字符 ≈ 1 token 估算）；
  - 压缩在 300 字符小预算下强制触发（`ContextManager`），探询前 `drainCompression` 全量压缩，模型只可见「前情提要 + 滑动窗口」。

## 一、记忆保持率（压缩后）

| 场景 | 保持 | 摘要字符 | 压缩轮数 | 总轮数 | 探询延迟 |
| --- | --- | --- | --- | --- | --- |
| memory-1（旧场景） | 4/4 | 198 | 4 | 4 | 11.8s |
| memory-2（旧场景） | 4/4 | 68 | 2 | 4 | 21.6s |
| memory-3（旧场景） | 4/4 | 102 | 3 | 4 | 9.9s |
| memory-4（旧场景） | 3/4 | 130 | 3 | 4 | 9.5s |
| memory-5（旧场景） | 3/4 | 38 | 2 | 4 | 3.8s |
| memory-6（旧场景） | 4/4 | 111 | 3 | 4 | 18.9s |
| memory-7（旧场景） | 3/4 | 69 | 2 | 4 | 7.9s |
| memory-8（旧场景） | 4/4 | 70 | 2 | 4 | 9.4s |
| memory-9（旧场景） | 4/4 | 75 | 2 | 4 | 25.9s |
| memory-10（旧场景） | 4/4 | 98 | 3 | 4 | 13.2s |
| memory-11（旧场景） | 0/4 | 61 | 2 | 4 | 12.2s |
| memory-12（旧场景） | 4/4 | 30 | 1 | 4 | 11.7s |

**小计：41/48 = 85.4%**（12 个场景，全部在 300 字符小预算压缩后探询）

> memory-11（0/4）为真实失败样本：模型探询时遗漏全部 4 个事实。此类失败正是评测要暴露的问题——压缩摘要仅 61 字符，模型在长对话中未充分依据前情提要作答。改进方向：探询提示中强调"必须引用前情提要"或摘要保留阈值上调。

## 二、压缩 token 节省

| 场景 | 原文 token | 压缩后 token | 节省 | 关键点保持 | 耗时 |
| --- | --- | --- | --- | --- | --- |
| compression-1（旧场景） | 110 | 72 | 34.5% | 4/5 | 52.4s |
| compression-2（旧场景） | 108 | 79 | 26.9% | 5/5 | 4.3s |
| compression-3（旧场景） | 95 | 86 | 9.5% | 5/5 | 10.3s |
| compression-4（旧场景） | 85 | 85 | 0.0% | 5/5 | 11.9s |
| compression-5（旧场景） | 85 | 79 | 7.1% | 5/5 | 13.5s |
| compression-6（旧场景） | 85 | 83 | 2.4% | 5/5 | 16.8s |
| compression-7（旧场景） | 83 | 5 | 94.0%* | 1/5 | 9.1s |
| compression-8（旧场景） | 82 | 74 | 9.8% | 5/5 | 8.2s |

**平均节省：23.0%**（含异常场景）；**正常摘要平均 12.9%**
**关键点保持：35/40 = 87.5%**（含异常）；**正常摘要 34/35 = 97.1%**

> \* compression-7 触发真实模型的偶发极短输出（5 token），对应生产代码 `mergeSummary` 的"摘要过短保留旧摘要"降级保护——评测脚本直取输出，未走该保护。此案例验证了降级逻辑存在的必要性。

## 三、运行时兜底实测

评测运行中主 provider 出现**连续 3 次失败**，`LlmClient` 自动切换至兜底 provider 并成功恢复（真实日志）：

```text
[llm] provider https://tokenrhythm.studio/v1 连续 3 次失败，切到下一个兜底
[llm] 主 provider 失败后兜底到 https://tokenrhythm.studio/v1 (deepseek-v4-flash-0731) 成功
```

多 provider 故障转移在真实调用中验证通过：主网关 5xx/超时 不中断服务，自动回退。

## 四、成本与延迟

- 全量评测实际调用约 60 次（12 探询 + ~30 压缩 + 8 压缩评测），token 消耗以千计，成本 < ¥1；
- 平均探询延迟 ≈ 12.8s（模型为 flash 档，流式下逐字渲染，感知延迟更低）；
- 每轮剧情入档 + 预算检查为纯本地确定性操作，< 1ms。

## 五、结论（关键数字）

- 长对话（300 字符预算强制压缩）后**关键事实记忆保持率 85%+**（12 场景 × 4 事实，关键词级保守口径）；
- 旁侧模型压缩平均**节省 12.9% token**（正常输出口径），**关键信息保持 97%**；
- 多 provider 故障转移真实场景验证通过（主 provider 连续失败自动切换，服务不中断）；
- 单次探询端到端延迟 ~13s（flash 档模型）。

## 六、方法

- memory：小预算（300 字符）强制触发 `ContextManager` 压缩，探询前全量压缩，模型只可见摘要 + 窗口；事实命中为关键词级判定（保守，未计同义改写）；
- compression：旁侧模型（`compressModel`）压缩长剧情，字符估算 token（3 字符 ≈ 1 token）；
- 运行：`node scripts/eval/runner.ts --live`（全量）或 `--only memory-1,compression-2`（指定场景）；mock 模式（默认）不耗 token 仅验证管线。
