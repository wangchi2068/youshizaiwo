services:
  wangdachui-pi:
    build: .
    container_name: wangdachui-pi
    ports:
      - "7620:7620"
    env_file:
      - .env
    environment:
      WANGDACHUI_PORT: "7620"
    volumes:
      - ./state:/app/state # 账本/存档/历史持久化（升级重建不丢档）
    restart: unless-stopped
